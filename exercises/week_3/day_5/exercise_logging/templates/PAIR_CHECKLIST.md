# Pair review checklist (Thursday)

**Reviewer name:** Curtis **Author / track:** Kaitlyn
- [x] Code runs without stack traces on happy path.
Yes Executes code as instructed by the exercies. Runs code as necessary
- [x] Collection choice matches README (Map / Set / Queue).
Yes Executes code code as instructed. Uses the functions as requested
- [x] No raw `System.out` debugging left in place of logging (after Phase 3).
There is system.out but it is ment the display results.
- [x] **equals/hashCode** if custom objects used inside **HashMap**/**HashSet** (N/A for String keys).
Yes.
- [x] One **specific** suggestion for readability or naming.
readability of code will help adding comments for me to quickly understand your intetions. It helps me pass what need to see as reviewer.

**Written feedback (2–3 sentences):**

The code is very solid and executes the objectives of the task in hand. One recomendation is to just provide the code commnents on the tasks it is running for reviewer to understand your intentions for tasks such as this.

**Reviewer name:** Kaitlyn **Author / track:** Curtis
- [x] Code runs without stack traces on happy path.
- [x] Collection choice matches README (Map / Set / Queue).
- [ ] No raw `System.out` debugging left in place of logging (after Phase 3).
- [ ] **equals/hashCode** if custom objects used inside **HashMap**/**HashSet** (N/A for String keys). (not applicable)
- [x] One **specific** suggestion for readability or naming.

**Written feedback (2–3 sentences):**

Logging still uses raw `System.out`. The predicate and comparator are clean and well-named. descriptionOrder might be a candidate for renaming as it doesn't describe how the descriptions are sorted (alphabetical/ascending/descending?), but renaming it isn't strictly necessary. 
